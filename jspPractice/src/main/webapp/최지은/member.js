/**
 * 
 */
 document.addEventListener('DOMContentLoaded', () => {
    const button = document.querySelector('input[type=submit]');
    
    button.addEventListener('click', (event) => {
        const id = document.querySelector('input[type=text]:nth-of-type(1)');
	    const password = document.querySelector('input[type=password]:nth-of-type(1)');
	    const password2 = document.querySelector('input[type=password]:nth-of-type(2)');
	    const name = document.querySelector('input[type=text]:nth-of-type(2)');
    });
});